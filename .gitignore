# ROS 2 / colcon
build/
install/
log/

# local build marker written by build.sh
.build_used_system_python

# Python
__pycache__/
*.py[cod]

# Model weights — students download these locally (see INSTRUCTIONS.md)
*.pt

# Internal reports (never committed to the course repo)
reports/
